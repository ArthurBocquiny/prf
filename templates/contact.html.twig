{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Nous contacter</h1>
    
    {% if app.user is null%}
        <form method="post" action="" ecntype="multipart/form-data"
            <label for="name">Nom</label>
            <input type="text" name="name" id="name" required>
            <label for="email">Email</label>
            <input type="email" name="email" id="email" required>
            <label for="object">Nature de votre demande</label>
            <input type="text" name="object" id="object" required>
            <label for="message">Votre message</label>
            <textarea name="lastname" id="message" name="message" required></textarea>
        </form>
    {% else %}
    {% endif %}
{% endblock %}